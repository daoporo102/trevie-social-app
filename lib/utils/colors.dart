import 'package:flutter/material.dart';

// Background colors
const mobileBackgroundColor = Color.fromRGBO(
  247,
  248,
  251,
  1,
); // soft off-white
const webBackgroundColor = Color.fromRGBO(255, 255, 255, 1); // pure white
const textFieldBackgroundColor = Color.fromRGBO(
  240,
  242,
  246,
  1,
); // subtle light gray for input textfield & search bars

const appPrimaryColor = Color.fromRGBO(
  34,
  197,
  94,
  1,
); // vibrant green for success actions
const primaryTextColor = Color.fromRGBO(
  20,
  20,
  20,
  1,
); // dark text for contrast on light bg
const secondaryColor = Color.fromRGBO(107, 114, 128, 1); // muted slate gray
const onPrimaryColor = Color.fromRGBO(255, 255, 255, 1); // white text color
